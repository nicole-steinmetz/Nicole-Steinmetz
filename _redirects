# Cloudflare Pages redirects
# Format: <from> <to> <status>

# Force the apex onto www (or delete this and do the reverse — pick one and
# be consistent, split hostnames cost you SEO).
https://nicolesteinmetz.com/*  https://www.nicolesteinmetz.com/:splat  301!

# ---------------------------------------------------------------------------
# UNBUILT ROUTES — holding pattern, 2026-08-31.
# Contact, FAQs and the six pricing pages are live from Figma.
# There is still no platform-decider, get-a-quote or start-a-project frame.
# 302, not 301: these URLs come back when those Figma pages are signed off.
# python3 -m http.server does not honour this file — local preview still 404s.
# ---------------------------------------------------------------------------
/platform-decider/    /  302
/get-a-quote/         /  302
/start-a-project/     /  302
